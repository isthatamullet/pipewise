"""Pipewise reference adapter for LangGraph.

Adapts a canonical ``create_react_agent`` graph into pipewise's ``PipelineRun``
schema. See ``adapter.py`` for the eval-time API surface (``load_run``,
``default_scorers``) and ``capture.py`` for the run-time capture primitive.
"""

__version__ = "0.1.0"
